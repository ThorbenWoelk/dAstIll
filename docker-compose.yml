services:
  dastill-monitor:
    build: .
    container_name: dastill-monitor
    volumes:
      # Mount data directory (set by docker-wrapper script)
      - ${DASTILL_HOST_DATA_PATH:-./data}:/app/data
      # Mount configuration directory
      - ./config:/app/config
      # Mount Claude Code authentication from host (needs write for updates)
      - ~/.claude:/root/.claude
      - ~/.claude.json:/root/.claude.json
      - ~/.ssh:/root/.ssh:ro
    environment:
      # Storage path inside container (set by docker-wrapper script)
      - DASTILL_BASE_PATH=${DASTILL_CONTAINER_DATA_PATH:-/app/data}
      # Configuration directory (use default app config location)
      - DASTILL_CONFIG_DIR=/app/config
      # Optional: Override check interval (default is from config)
      # - DASTILL_CHECK_INTERVAL=300
    env_file:
      - .env
    restart: unless-stopped
    # Health check to ensure service is running
    healthcheck:
      test: ["CMD", "pgrep", "python"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Optional: Add a second service for one-time operations
  dastill-cli:
    build: .
    container_name: dastill-cli
    volumes:
      # Mount data directory (set by docker-wrapper script)
      - ${DASTILL_HOST_DATA_PATH:-./data}:/app/data
      # Mount configuration directory
      - ./config:/app/config
      # Mount Claude Code authentication from host (needs write for updates)
      - ~/.claude:/root/.claude
      - ~/.claude.json:/root/.claude.json
      - ~/.ssh:/root/.ssh:ro
    environment:
      # Storage path inside container (set by docker-wrapper script)
      - DASTILL_BASE_PATH=${DASTILL_CONTAINER_DATA_PATH:-/app/data}
      - DASTILL_CONFIG_DIR=/app/config
    env_file:
      - .env
    # Override entrypoint for CLI usage
    entrypoint: ["uv", "run", "python", "main.py"]
    # This service doesn't run by default
    profiles: ["cli"]
