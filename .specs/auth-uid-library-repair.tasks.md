# Tasks: Auth UID Library Repair

## Current State
The current Firebase UID for `thorben.woelk@gmail.com` now has the repaired five-channel subscription set, 44 migrated video-state records, and the merged preferences document with the expected channel order. The remaining step is user-facing verification after refresh/sign-in.

## Steps
- [x] Confirm the current Firebase UID for `thorben.woelk@gmail.com`
- [x] Compare old/new UID-scoped subscription, video-state, and preferences data
- [x] Copy the old UID-scoped library data onto the current UID
- [x] Verify the current UID now has the expected records
