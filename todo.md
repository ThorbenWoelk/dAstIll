- add some channels to the subscriptions: TheDiaryOfACEO, GoobieAndDoobie, CosmicSkeptic, RayFernando1337, NateBJones, 3blue1brown. Note that we will run in a youtube limit when starting the app. do we have a nice way to handle this already? we don't want to limit the number of videos. instead, we want to stretch download over time to be okay on the api and not get blocked. web search how best to get around the limits. 