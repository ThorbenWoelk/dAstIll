[advisories]
ignore = [
    "RUSTSEC-2026-0049", # rustls-webpki faulty matching logic in older rustls dependency lines
    "RUSTSEC-2026-0098", # rustls-webpki name constraints in older rustls dependency lines
    "RUSTSEC-2026-0099", # rustls-webpki wildcard constraints in older rustls dependency lines
    "RUSTSEC-2025-0012", # backoff 0.4.0 unmaintained - transitive dep of firestore 0.48.0 (latest); no fix available upstream
    "RUSTSEC-2024-0384", # instant 0.1.13 unmaintained - transitive dep of backoff via firestore; same root cause
]
