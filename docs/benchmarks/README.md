# Benchmarks

The public docs page for this section is [`index.md`](./index.md).

Store dated benchmark notes here when measuring indexing or retrieval performance.

Use file names like `search-indexing-YYYY-MM-DD.md` and include:

- environment details
- dataset scope
- command or procedure used
- measured wall-clock time
- CPU / memory observations
- any notable caveats
