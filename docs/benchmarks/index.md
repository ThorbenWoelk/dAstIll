# Benchmarks

Benchmark notes live here when indexing or retrieval performance is measured.

Use dated files such as `search-indexing-YYYY-MM-DD.md`. Each note should include:

- environment details
- dataset scope
- command or procedure used
- measured wall-clock time
- CPU and memory observations
- caveats that affect comparison
